<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:meteo="http://schemas.android.com/apk/res-auto">

    <!-- More -->
    <item
        android:id="@+id/a_More"
        meteo:showAsAction="always"
        android:title="More"
        android:icon="@drawable/ic_action_more">
        <menu>

            <item android:id="@+id/action_settings"
                android:title="@string/action_settings"
                android:orderInCategory="200"
                meteo:showAsAction="never"
                android:icon="@drawable/ic_action_setting" />
            <item android:id="@+id/action_update"
                android:title="@string/action_update"
                android:orderInCategory="100"
                meteo:showAsAction="never"
                android:icon="@drawable/ic_action_refresh" />
            <item android:id="@+id/action_info"
                android:title="@string/action_info"
                android:orderInCategory="300"
                meteo:showAsAction="never"
                android:icon="@drawable/ic_action_info" />

        </menu>
    </item>

</menu>
